# 🚀 GitHub Pages 部署指南

## 第一步：生成 Personal Access Token

### 1. 访问 GitHub Settings
打开浏览器，访问：https://github.com/settings/tokens

### 2. 点击 "Generate new token (classic)"
页面右上角有一个 "Generate new token" 按钮，点击它，选择 "Generate new token (classic)"

### 3. 填写 Token 信息
- **Note**: 填写 `Spanish Learning App Deploy`（随便填）
- **Expiration**: 选择 `No expiration`（永不过期）或合适的时间
- **Select scopes**: 勾选 ✅ `repo` 下面的所有选项

### 4. 生成 Token
点击页面底部的 "Generate token" 按钮

### 5. 复制 Token
生成后，你会看到一个类似这样的字符串：
```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
**重要**：复制并保存好这个Token，它只会显示一次！

---

## 第二步：将 Token 提供给我

生成Token后，直接告诉我，我会帮您：
1. 将代码推送到您的GitHub仓库
2. 配置GitHub Pages
3. 生成访问链接

---

## 第三步：女朋友安装应用（稍后）

部署完成后，我会提供详细教程让她安装到手机上。

---

## ❓ 常见问题

### Q: Token 有什么用？
A: Token 就像是"钥匙"，让我可以代替您操作GitHub仓库。

### Q: Token 安全吗？
A: 只要不泄露给他人就是安全的。如果泄露了，可以在GitHub上 revoke（撤销）这个Token。

### Q: 我没有看到 Generate new token 按钮？
A: 确保你登录了GitHub账号，并点击右上角的头像进入 Settings。

---

准备好Token后，直接发给我即可！